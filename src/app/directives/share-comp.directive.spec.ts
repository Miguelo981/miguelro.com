import { ShareCompDirective } from './share-comp.directive';

describe('ShareCompDirective', () => {
  it('should create an instance', () => {
    const directive = new ShareCompDirective();
    expect(directive).toBeTruthy();
  });
});
